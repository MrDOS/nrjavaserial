plugins {
	id 'java-gradle-plugin'
}

gradlePlugin {
	plugins {
		register('native-library-binary-fetcher') {
			id = 'native-library-binary-fetcher'
			implementationClass = 'FetchNativeLibraryBinariesPlugin'
		}
	}
}
